[project]
name = "reportops-headless"
version = "0.1.0"
description = "Headless Modal workflow for agency report generation and Gmail approval loops"
requires-python = ">=3.11"
dependencies = [
  "modal>=1.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests_py"]

